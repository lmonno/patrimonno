"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#43A047",
    },
    secondary: {
      main: "#388E3C",
    },
  },
});

export default theme;
